<!DOCTYPE html>
<html>
  <%= render 'shared/header_tag' %>
  <body>
    <%= render 'shared/header' %>
    <div class="body">
      <p id="notice"><%= notice %></p>
      <%= yield %>
    </div>
  </body>
</html>
