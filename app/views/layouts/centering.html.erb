<!DOCTYPE html>
<html>
  <%= render 'shared/header_tag' %>
  <body>
    <%= render 'shared/header' %>
    <div class="body">
      <div class="container">
        <p id="notice"><%= notice %></p>
        <div class="row">
          <div class="col-md-6 col-md-offset-3">
            <%= yield %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
