$('.fav-count[data-book-id="<%= @book.id %>"]').replaceWith('<%= j favorite_link(@book, current_user) %>');
$('.fav-users[data-book-id="<%= @book.id %>"]').replaceWith('<%= j render 'pickup', book: @book %>');
